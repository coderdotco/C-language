#include<stdio.h>
int main(){
    // FILE* ptr=fopen("trp.txt","r");
    // char str[100];

    // // fgets(<string pointer > (str), <string size> (100) ,<file pointer> (ptr) )
    // while(fgets(str,100,ptr) !=NULL){
    //     printf("%s",str);
    // }

    //creating a file
    FILE* ptr=fopen("trp1.txt","w"); // for creating a file open the file in write(w) mode
    
}