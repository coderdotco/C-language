#include<stdio.h>

int main(){
    int r=6;
    int h=7;
    printf("the area of circle with radius 6 is %f \n", 3.14*r*r);
    printf("the volume of cylinder with radius %d height %d is %f",r,h,3.14*r*r*h);
    return 0;
}